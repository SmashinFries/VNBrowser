import AnimatedStack from '@/components/headers';
import { Stack } from 'expo-router';

const ReleaseLayout = () => {
	return <AnimatedStack screenOptions={{ title: 'Release' }} />;
};

export default ReleaseLayout;
