import AnimatedStack from '@/components/headers';
import { Stack } from 'expo-router';

const ReleasesLayout = () => {
	return <AnimatedStack screenOptions={{ title: 'Releases' }} />;
};

export default ReleasesLayout;
