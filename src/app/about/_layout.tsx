import AnimatedStack from '@/components/headers';
import { Stack } from 'expo-router';

const AboutLayout = () => {
    return <AnimatedStack screenOptions={{ title: 'About' }} />;
};

export default AboutLayout;
